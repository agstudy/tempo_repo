library(shiny)



# adveq colors: use when assigning color to data points
# reclycle the vector if more colors needed
# red is the first color
adveq_colors <- c('#ce1126', '#003399', '#ff9900', '#006633', '#800080',
                  '#99CC00', '#3366ff', '#ffcc00', '#de007b', '#009fee',
                  '#ff5f00', '#808000')


shinyServer(function(input, output, session) {

    # empty, since there are no inputs and output
    # use session if need to update the controls
    
    
})

